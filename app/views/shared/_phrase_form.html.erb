<%= form_for @phrase do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class="field">
    <%= f.text_area :phrase %>
  </div>
  <div class="actions">
    <%= f.submit "Submit" %>
  </div>
<% end %>